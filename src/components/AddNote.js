import React from 'react';
import PropTypes from 'prop-types';

// addNote.propTypes = {
    
// };

function AddNote(props) {
    return (
        <div>
            adđ d
        </div>
    );
}

export default AddNote;