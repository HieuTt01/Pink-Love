import React from 'react';
import PropTypes from 'prop-types';

// search.propTypes = {
    
// };

function Search(props) {
    return (
        <div>
            search
        </div>
    );
}

export default Search;